server:
  port: 9527
  servlet:
    context-path: /api

spring:
  application:
    name: yanManus
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY:}
      chat:
        options:
          model: qwen-plus


